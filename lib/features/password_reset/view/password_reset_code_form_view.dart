import 'package:flutter/material.dart';

class PasswordResetCodeFormView extends StatelessWidget {
  const PasswordResetCodeFormView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Code form');
  }
}
