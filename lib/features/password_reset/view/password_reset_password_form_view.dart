import 'package:flutter/material.dart';

class PasswordResetPasswordFormView extends StatelessWidget {
  const PasswordResetPasswordFormView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Password form');
  }
}
